# Create Component: $ARGUMENTS

Review the components in the `prompts` directory and step through them one-by-one to build the **$ARGUMENTS** component.

## Instructions

1. Read each prompt file in the `prompts/` directory in order (step-1a, step-1b, step-1c, step-2a, step-2b, step-2c, step-2d, step-2e)
2. Follow the instructions in each prompt file sequentially
3. Apply the guidance from each step to build the `$ARGUMENTS` component
4. Complete each step before moving to the next

## Component to Build

**$ARGUMENTS**
